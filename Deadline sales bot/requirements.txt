fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.27.2
# Tenant config loader (Phase 1, 2026-05-26) — reads tenants/<slug>/config.yaml
pyyaml==6.0.2

# LangChain stack
langchain==0.3.7
langchain-openai==0.2.5
langchain-chroma==0.1.4
langchain-postgres==0.0.12
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2

# Vector DB + embeddings
chromadb==0.5.18
sentence-transformers==3.2.1

# Postgres + pgvector (Week 1: migrate from Chroma)
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
# pgvector pinned to 0.2.5 to match langchain-postgres==0.0.12 constraint.
# Both 0.2.5 and 0.3.6 expose the SQLAlchemy `Vector` type used in db/models.py.
pgvector==0.2.5
alembic==1.13.3

# Dev / testing — kept in main requirements so Railway image can run pytest
# on demand for ops debugging. ~200 KB total, negligible image bloat.
pytest==8.3.3
pytest-asyncio==0.24.0
