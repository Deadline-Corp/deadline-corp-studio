# Аудит системы — 2026-06-16 (verified synthesis)

Глубокий аудит всей AI-CRM (6 параллельных агентов прошли код по осям: устойчивость,
каналы, баги, ядро, UI, документация). **Ниже — ПРОВЕРЕННАЯ выжимка:** автоскан дал
ряд ложных срабатываний (галлюцинированные file:line), они отсеяны.

## Ложные тревоги автоскана (проверено руками — НЕ баги)

| Заявка скана | Реальность |
|---|---|
| «`wa_autonomous` не существует → автопилот мёртвый» | Поле ЕСТЬ (миграция 017, db/models.py). Используется в _conv_summary_row. |
| «next_action.py упадёт на None от LLM» | Есть гард `if not data: return {}` (next_action.py:101). Безопасно. |
| «воронка захардкожена на 7 стадий (funnel.py:35)» | `_STAGE_ORDER` (conversation_brain/whatsapp_sync) содержит все 12 стадий вкл. nda/tz_approved/in_work/post_sale. Строки 35-36 в funnel.py не содержат `_FORWARD`. |
| «merge_wa_split race → потеря данных» | Один инстанс Railway, крон последователен; реальный риск низкий. |

**Вывод: ядро крепче, чем показал автоскан.**

## ✅ Что работает хорошо (подтверждено)

- **Ядро = чистые детерминированные функции:** воронка (иерархия оператор>классификатор>авто),
  скоринг/температура (невозвратный апгрейд + decay), классификатор (LLM + эвристика-фоллбэк).
- **Защита от галлюцинаций LLM в КОДЕ, не в промпте:** стражи созвонов
  (_mentions_call/_defers_timing/_lead_silent), детерминированный расчёт дат, анти-утечка мета-анализа.
- **Мозги:** pgvector + HNSW, чанкинг, hot-reload промптов (TTL 60с + валидация), runtime-переиндексация.
- **Устойчивость:** watchdog (>120с → рестарт), per-chat локи, ack-first вебхуки, тяжёлое в потоки.
- **Одно окно ~90%:** единый Drawer из Inbox/Funnel/Canvas/Tasks/Calendar.
- **Адаптация под бизнес заложена:** 7 нишевых пресетов, онбординг-генератор (анкета→LLM→черновик),
  демо-данные, мультиязычность, tenant-конфиги.

## ⚠️ Что реально слабо (по приоритету)

1. **🔴 Подключение каналов в панели** — главный разрыв с видением. Адаптеры+вебхуки готовы,
   но токены вбиваются в Railway вручную, webhook-URL негде скопировать, нет кнопки «Тест».
   Channels.tsx показывает только «подключён/нет». Нужны эндпоинты + расширенная страница.
2. **🟡 Бизнес-документация** — README устарел (Chroma/Haiku/один клиент); нет ONBOARDING_GUIDE
   и ADMIN_UI_GUIDE. Технические доки крепкие, но не-технический клиент не заведёт сам.
3. **🟡 Видимость ошибок** — ~40 `except: pass` без логов → не видно, почему черновик/созвон не сработал.
4. **🟡 Идемпотентность вебхуков** — устойчивый дедуп только у Telegram; WhatsApp/IG могут
   обработать сообщение дважды при сетевом ретрае.
5. **🟢 UI-полировка** — ConversationDrawer (572 стр.) и Settings (828 стр.) монолиты; черновик
   бота и ответ оператора в одном поле без явной пометки; раскрытые секции карточки не запоминаются.
6. **🟢 KB-гигиена на будущее** — нет версий/одобрения чанков → риск разбухания дублями на росте.

## 🗺 План (по ценности)

| # | Что | Объём |
|---|---|---|
| 1 | Страница «Каналы»: статус + webhook-URL (копировать) + поле токена + кнопка «Тест» + бэкенд-эндпоинты | средне |
| 2 | Бизнес-доки: ONBOARDING_GUIDE + ADMIN_UI_GUIDE + обновить README | средне |
| 3 | Логирование ошибок (видимость) + идемпотентность WA/IG | низко-средне |
| 4 | UI-полировка: разбить Drawer/Settings, явная пометка черновика, память секций | средне |
| 5 | KB: версии + одобрение чанков | средне |

Старт рекомендуется с **#1 (Каналы)** — прямое попадание в «одно окно + лёгкое подключение».
